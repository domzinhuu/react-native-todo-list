import { StyleSheet } from "react-native";
import { base } from "./src/themes/base";

export const styles = StyleSheet.create({
  container: {
    backgroundColor: base.colors.gray[600],
    height: "100%",
  },
});
