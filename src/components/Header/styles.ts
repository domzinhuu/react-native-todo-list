import { StyleSheet } from "react-native";
import { base } from "../../themes/base";

export const styles = StyleSheet.create({
  container: {
    height: 194,
    backgroundColor: base.colors["gray"]["700"],
    justifyContent: "center",
    alignItems: "center",
  },
});
